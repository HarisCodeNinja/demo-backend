export interface CreateDesignationInput {
	designationName: string;
}

export interface UpdateDesignationInput {
	designationName: string;
}

export interface QueryDesignationInput {
	page : number;
	pageSize: number;

}
